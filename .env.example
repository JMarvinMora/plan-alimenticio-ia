# Variables de entorno para la app de plan alimenticio

# API host (puede ser azure, ollama, openai o github)
API_HOST=github

# Token de acceso de GitHub (reemplazar con uno real en .env local o en secrets)
GITHUB_TOKEN=your_token_here

# Modelo por defecto
GITHUB_MODEL=gpt-4o-mini